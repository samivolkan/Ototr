import fs from 'node:fs';import vm from 'node:vm';const s=fs.readFileSync(new URL('../task-merkezi/index.html',import.meta.url),'utf8'),m=s.match(/const DEFAULT_CATS=(\[[\s\S]*?\]);\s*const RAW=(\[[\s\S]*?\]);/),ctx={};vm.runInNewContext(`C=${m[1]};R=${m[2]}`,ctx);let n=0;for(const[ci,g]of ctx.R.entries())for(const[ti,title]of g.entries()){n++;const q=title.replaceAll("'","''");console.log(`  ('00000000-0000-4000-8000-${String(n).padStart(12,'0')}'::uuid,'10000000-0000-4000-8000-000000000001'::uuid,'20000000-0000-4000-8000-${String(ci+1).padStart(12,'0')}'::uuid,'${q}','medium',${ti},false,false)${n<100?',':''}`)}console.error(`COUNT ${n}`);
